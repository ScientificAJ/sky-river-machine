{
  "name": "sky-river-machine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:chromium": "node scripts/build-browser.mjs chromium",
    "build:firefox": "node scripts/build-browser.mjs firefox",
    "package:chromium": "npm run build:chromium && node scripts/package-browser.mjs chromium",
    "package:firefox": "npm run build:firefox && node scripts/package-browser.mjs firefox",
    "package:all": "npm run package:chromium && npm run package:firefox",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "audit": "node scripts/audit-build.mjs",
    "evaluate:model": "node scripts/evaluate-model.mjs",
    "evaluate:embedding": "node scripts/evaluate-embedding.mjs",
    "check": "npm run typecheck && npm test && npm run build:chromium && npm run build:firefox && npm run audit"
  },
  "dependencies": {
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^26.1.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
